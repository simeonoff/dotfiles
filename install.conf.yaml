- defaults:
   link:
      create: true
      relink: true

- clean: ['~']

- link:
   ~/.dotfiles: ''
   ~/.config/nvim:
      force: true
      path: nvim/
   ~/.zshrc:
      force: true
      path: zshrc
   ~/.vim:
      force: true
      path: vim/

- shell: []

