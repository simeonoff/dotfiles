- defaults:
   link:
      create: true
      relink: true

- clean: ['~']

- link:
   ~/.dotfiles: ''
   ~/.config/alacritty:
   ~/.bashrc:
   ~/.config/nvim:
   ~/.gitconfig:
   ~/.config/neofetch:
   ~/.config/ranger:
   ~/.config/redshift.conf:
   ~/.config/rofi:
   ~/.tmux:
   ~/.tmux.conf:
   ~/.vim:
   ~/.zshrc:

- shell: []

